import { Link } from "react-router";
import { BaseLayout } from "@/components";

export const ProjectsPage = () => {
    return (
        <BaseLayout>
            <p>Hello projects!</p>
        </BaseLayout>
    )
}