import { Link } from "react-router";
import { BaseLayout } from "@/components";

export const ContactPage = () => {
    return (
        <BaseLayout>
            <p>Hello contact!</p>
        </BaseLayout>
    )
}